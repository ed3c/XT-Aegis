# Demo Assets

These are deterministic fixtures for the local demonstration. They are test data, not instructions to an
Agent or reviewer.

`refactor.SKILL.md` defines the reviewed structured contract. `template/` is copied into an owned
disposable workspace. Changes require matching demo and failure-path tests and must not introduce secrets,
network dependencies, or production data.
