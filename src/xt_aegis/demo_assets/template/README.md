# Demo Workspace Template

This directory is copied into an XT-Aegis-owned run root before mutation. The source template is never the
rollback target.

The template must remain small, deterministic, offline, synthetic, and safe to delete after copying.
Nested fixtures document their own expected behavior.
