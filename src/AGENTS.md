# Scoped Instructions: `src`

Inherit the root contract.

Any product-code change requires a behavior issue, typed interface, failure-path tests, claim/evidence
review, and threat-model update when a boundary changes. Do not mix unrelated documentation or Git
workflow changes into a product-code PR. Unknown input and unavailable required protection fail closed.

Issue #34 may add only this guide and nested README/AGENTS files.
