# Scoped Instructions: `docs/adr`

Inherit `docs/AGENTS.md`.

An ADR change must include context, decision, alternatives, consequences, status, intent IDs, and owning
issue. It must not claim implementation solely because a decision was accepted. New runtime authority,
identity, isolation, network, credential, persistence, or remote mutation boundaries require threat-model
review and negative-test plans.
